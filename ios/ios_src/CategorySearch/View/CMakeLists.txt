set(headers ${headers} src/CategorySearch/View
)
set(currsources
  src/CategorySearch/View/CategorySearchMenuOptionsFactory.cpp
)

set(sources ${sources} ${currsources})
source_group(\\ios_src\\\\CategorySearch\\View FILES ${currsources})
