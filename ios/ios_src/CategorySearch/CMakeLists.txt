set(headers ${headers} ios_src/CategorySearch
)
set(currsources
)
set(sources ${sources} ${currsources})
source_group(\\ios_src\\\\CategorySearch FILES ${currsources})
include(ios_src/CategorySearch/View/CMakeLists.txt)
