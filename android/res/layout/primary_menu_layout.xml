<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:id="@+id/primary_menu_view"
    android:clipChildren="false"
    android:clipToPadding="false">

    <RelativeLayout
    	android:id="@+id/primary_menu_view_list_container"
        android:layout_marginTop="40dip"
	    android:layout_width="180dip"
	    android:layout_height="fill_parent"
	    android:orientation="horizontal"
	    android:clickable="true"
		android:focusable="true"
		android:focusableInTouchMode="true"
	    android:clipChildren="true"
	    android:clipToPadding="false" >
	    
        <RelativeLayout 
            android:background="@color/white"
            android:layout_width="fill_parent"
            android:layout_height="50dp"/>
	    
	    <ListView
	        android:id="@+id/primary_menu_item_list"
	        android:layout_marginTop="50dp"
	        android:layout_width="fill_parent"
	        android:layout_height="wrap_content"
	        android:dividerHeight="0dp"
			android:divider="@null"
			android:listSelector="@android:color/transparent"
			android:background="@android:color/transparent"
			android:cacheColorHint="@android:color/transparent"
			android:textColor="@color/white"
		    android:clipChildren="false"
		    android:clipToPadding="false"/>
	    
	</RelativeLayout>
	
    <RelativeLayout
        android:id="@+id/primary_menu_drag_tab_view"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="180dip"
        android:layout_marginTop="40dip"
        android:background="@color/gold"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true" >
	    
       <ImageView
           	android:contentDescription="@string/icon_me"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerInParent="true"
            android:src="@drawable/icon0_me_icon" />

	</RelativeLayout>
	
    <RelativeLayout
        android:layout_width="230dp"
        android:layout_height="10dp"
        android:layout_alignParentLeft="false"
        android:layout_alignParentRight="false"
        android:layout_alignParentTop="false"
        android:layout_below="@id/primary_menu_drag_tab_view"
        android:scaleType="fitXY"
        android:background="@drawable/shadow_01" />
	
</RelativeLayout>
